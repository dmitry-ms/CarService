@model IEnumerable<CarService.Web.Models.ClientCarVM>

@{
    ViewData["Title"] = "Ваш гараж";
}

<div class="d-flex justify-content-between mb-5">
    <div><h3>Ваш гараж</h3></div>
    <div>
        <a class="btn btn-success btn-lg mr-2" asp-action="AddCar">Добавить автомобиль</a>
    </div>
</div>


@if (Model.Any())
{
    <div class="row row-cols-1 row-cols-md-2 g-4">
        @foreach (var item in Model)
        {
            <div class="col">
                <div class="card">
                    @*<img src="" class="card-img-top" alt="...">*@
                    <div class="card-body">
                        <h5 class="card-title">@Html.DisplayFor(modelItem => item.BrandName) @Html.DisplayFor(modelItem => item.ModelName)</h5>
                        <p class="card-text">@Html.DisplayFor(modelItem => item.MileageKM)</p>
                        <a href="#" class="btn btn-outline-primary stretched-link float-end">Сервис</a>
                    </div>
                </div>
            </div>
        }
    </div>





    @*<table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.BrandName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ModelName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.MileageKM)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.BrandName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ModelName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MileageKM)
                        </td>
                        <td>
                            @Html.ActionLink("Изменить", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("Подробней", "Details", new { id = item.Id }) |
                            @Html.ActionLink("Удалить", "Delete", new { id = item.Id })
                        </td>
                    </tr>
                }
            </tbody>
        </table>*@
}
else
{
    <div class="row justify-content-center mt-5 p-5">
        <div class="col-6 text-center">
            <h2>У вас пока нету автомобилей, <a class="nav-link" asp-action="AddCar">нажмите чтобы добавить.</a></h2>
        </div>
    </div>
}
